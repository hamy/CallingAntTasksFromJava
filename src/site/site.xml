<?xml version="1.0" encoding="UTF-8"?>
<project name="${project.name}">

	<bannerLeft>
		<name>${project.name}</name>
	</bannerLeft>

	<publishDate format="yyyy-MMMM-dd HH:mm:ss" />

	<body>

		<links>
			<item name="Github" href="${project.scm.url}" />
		</links>

		<!-- side bar menu -->
		<menu name="About">
			<item name="Home" href="index.html" />
			<item name="Preparations" href="preparations.html" />
		</menu>

		<!-- reports -->
		${reports}

	</body>

</project>